"use client";

import { display } from "@/fonts"

export default function WIP() {
    return (
        <div className="w-full h-fit bg-blue-700 opacity-50 hover:opacity-100 fixed bottom-0 left-0 flex items-center py-2">
            <p className="text-sm mx-auto text-white">Hey, you've found my page! It's still in the kitchen - stay tuned as the rest gets cooked up! 🧑‍🍳</p>
        </div>
    );
}